# 会话上下文摘要（compact 产物）

> 生成时间：2026-09-23 ｜ 依据技能：`extensions/skills/context_compact/compact.md`
> **用途**：作为新一轮会话的起点上下文。后续思考与执行**仅基于本摘要**，
> 不再引用被清理掉的旧对话、旧日志、被推翻的方案。
> 本文件是**摘要**，不是事实源；与 `docs/architecture.md` / `docs/设计逻辑.md` 冲突时以后者为准。

---

## 1. 当前任务目标

按 Pi harness 的四层架构，用 Python 从零实现一个 agent harness（项目名 `sigma`），
分阶段推进 **P0 → P5**，每阶段先出计划文档、经用户审阅后才落码。

## 2. 硬性核心约束（不可协商）

| # | 约束 | 出处 |
| --- | --- | --- |
| 1 | Python 3 实现 | `AGENTS.md` §1 |
| 2 | 面向对象 + 继承制，为后期扩展留位 | `AGENTS.md` §2 |
| 3 | 所有方法参数/返回值必须有类型注解；元数据尽量用 `BaseModel` | `AGENTS.md` §3 |
| 4 | 架构有疑问 → 查 `pi-harness研究笔记.md`（根目录与 `docs/` 各一份） | `AGENTS.md` §4 |
| 5 | **关键点必须征询用户**，不自行拍板 | `AGENTS.md` §5 |
| 6 | **动手前先出计划文档给用户审阅**，由用户决定可行性 | `AGENTS.md` §7 |
| 7 | 五层单向依赖；兄弟层互不依赖；core 不碰 `evals` / `extensions` | 设计逻辑·不变式 |
| 8 | 常驻区（系统提示词 / 工具 schema / `AGENTS.md` / 技能索引）会话内**逐字节稳定** | 设计逻辑·不变式 |
| 9 | 唯一通道：agent 层 → LLM 层只有 `convert_to_llm()` 一个引用点 | 设计逻辑·不变式 |
| 10 | 宁可崩，不要错：认不出的消息类型不静默丢弃；抽象方法不给默认实现 | 设计逻辑·不变式 |
| 11 | 丢弃必须可见：被过滤/截断的内容，计数写回模型可见的 `content` | 设计逻辑·不变式 |
| 12 | 每个主张都要有数据；**证伪不了就不是门槛**（写了断言但无注入能让它变红 = 名义门槛） | 设计逻辑·不变式 |
| 13 | 抽象基类判据：**只有一个子类的抽象基类 = 纯间接，要删**；被真实出现的第二个实现要求时才抽 | 设计逻辑·落地说明 |

## 3. 已确认的关键决策

- **D1 语言**：Python（已拍板）。代价：拿不到 Pi 的零构建热重载。
- **D2 harness 边界**、**D3 扩展层形态**：已拍板。
- **D4 上下文预算**：常驻区总上限 **3500**；分项上限在 P4-批次1 重新分配过
  （旧五条相加 4050，从来没装下过；系统提示词一项收紧到 800 是为给 P4 技能索引留空间）。
- **D5 安全边界**、**D6 不做容器隔离**：状态仍为「待确认」。
- **协议**：P1 只做 OpenAI 兼容一套；Anthropic 排到 P4 之后；Google / Bedrock / Azure 永久不做。
- **`sigma_session` 不抽 `base.py`**：这是决定不是遗漏（只有 JSONL 一个实现）。
- **L3 钩子规则**（危险命令匹配）未做，属 P3-批次2，需要 `HookManager`；
  架构 6.3 节「挡不住什么」清单**一字未改，全部成立**。

## 4. 当前已完成进度

| 阶段 | 状态 |
| --- | --- |
| P0 骨架 | ✅ 已成形态 |
| P1 最小闭环（含批次 1–8：核心 5 工具、流式 CLI、联网搜索、联网精读） | ✅ 已完成 |
| P2-1 回放场景与评测运行器 | ✅ 已完成（图 7） |
| P2-2 会话树与存储 | ✅ 已完成（图 9 / 10） |
| P2-3 常驻区预算与两道断言 | ✅ 已完成（图 11） |
| P2-4 上下文压缩（视图式，不改历史） | ✅ 已完成（图 12） |
| P2-5 会话接续 + 注入框架加固 | ✅ 已完成（图 13） |
| P2-6 评测任务集 | ⚠️ **只落方案，未落码**（需真实 key + 判定脚本） |
| P3-批次1 工具安全边界（L1 写路径约束 + L2 影子 git checkpoint） | ✅ 已完成 |
| P3-批次2 钩子（L3） | ⬜ 未开始 |
| P4-批次1 Skill 系统（索引常驻 + 正文按需，门槛 G75–G79） | ✅ 已完成（图 14） |
| P4 其余（扩展加载 / 热重载 / 按名解析契约） | ⬜ 未开始 |

**最近提交**：`91ecd8c` G79（截断说明自己不能被预算吃掉，修一个真实缺陷）+ 文档数字更正。

## 5. 现存未解决问题

1. **`docs/decisions/_待填清单.md` 未填**：D1/D2/D3 的「判断依据」「我接受的代价」两栏
   必须**用户本人**填写（AI 不代填，面试会问"你为什么这么选"）；D4/D5/D6 待拍板后再填。
2. **P2-6 评测任务集未落码**：70 条任务集需要真实 API key 与判定脚本。
3. **一道测不了的验收门**：压缩成功率 ≤5% 那道门离线做不到，需真模型（已诚实记录）。
4. **L3 钩子规则未做**，安全边界 6.3 节清单仍全部成立。
5. **工作区未提交的改动**：`extensions/skills/context_compact/`（新技能）、
   根目录 `pi-harness研究笔记.md`（与 `docs/` 下那份重复）、`scripts/desktop/`。

## 6. 后续下一步行动

1. 等用户对 **P4 后续批次**（扩展加载 / 热重载 / 按名解析契约）出计划文档并审阅。
2. 或推进 **P3-批次2（HookManager + L3 钩子规则）**。
3. 提醒用户填写 `_待填清单.md` 的 D1–D3 两栏。
4. 清理工作区未提交项（尤其根目录那份重复的研究笔记，需确认是否删除）。

---

## 附：被清理掉的内容（不再引用）

- 各批次实现过程中的逐条调试日志、中间报错与试错记录；
- 已被推翻的旧方案（如 G51 预判、Q5 口径错误、旧五条预算上限相加 4050 的算法）；
- 已解决的临时问题（CI 本地绿/CI 红、mypy 目标平台、时间戳格式等）；
- 与当前任务无关的发散讨论。
