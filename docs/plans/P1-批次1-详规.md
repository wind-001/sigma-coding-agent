# P1 批次 1 详规：`sigma_ai`（Provider 层 + LLM 消息模型）

> 依据：`docs/architecture.md` 第 4.0 / 4.1 节、`docs/plans/P1-最小闭环.md` 批次 1
> 状态：**待本人审阅**（`AGENTS.md` 第 7 条）
> 提出日期：2026-09-20
> 前置：批次 0 已完成（提交 `df16835`，门禁在项目 `.venv` 下可复现）

---

## 0. 本批次要回答的问题

**一句话**：让 sigma 能对着一个真实的 provider 说一句话、拿到一个流式回答，
并且这个过程**离线可测、错误可归一化、消息不丢字段**。

本批次**不接触** loop、工具、会话树。它只做"最底下那一层管道"，
但这一层有一个真实的坑：**provider 私有签名字段**。

---

## 1. 交付清单（8 个文件）

| # | 文件 | 内容 | 验收方式 |
| --- | --- | --- | --- |
| 1 | `core/sigma_ai/messages.py` | LLM 层：4 消息 + 4 内容块 | 序列化往返 + `*_signature` 保真 |
| 2 | `core/sigma_ai/base.py` | `BaseProvider(ABC)`、`Usage`、`CancelToken` | 直接实例化抛 `TypeError` |
| 3 | `core/sigma_ai/events.py` | `StreamEvent` 判别联合（5 类） | 逐类 round-trip |
| 4 | `core/sigma_ai/errors.py` | `ProviderError` + 5 错误码 | 错误码映射单测 |
| 5 | `core/sigma_ai/fake.py` | `FakeProvider`，按 transcript 回放 | 两次回放逐字节一致 |
| 6 | `core/sigma_ai/openai_compat.py` | `OpenAICompatProvider` | 单测不联网（用 mock transport） |
| 7 | `core/sigma_ai/tokens.py` | `estimate_tokens` 的保守估算 | 与真实 usage 的比例校准（记录用） |
| 8 | `tests/test_sigma_ai_*.py` | 上述全部单测 | 无 API key 也全绿 |

**注意第 7 项**：`tokens.py` 是**估算**，不是账本。理由见第 4 节。

---

## 2. 逐项规格

### 2.1 `messages.py` —— 四个消息模型

```python
class SystemMessage(BaseModel):
    """system 消息。

    ⚠️ **不要往这里放压缩摘要。**

    摘要必须降级成 UserMessage（架构方案 4.0.6 节）。理由：system 消息进常驻区，
    而常驻区在会话内必须逐字节稳定（D4）；中途变化会让 prompt cache 从变动点起
    全部失效。见本文件 G9。

    本类字段集合由单测断言固定，新增字段会导致测试失败——这是有意的。
    """

    role: Literal["system"] = "system"
    content: str | list[TextBlock]
    # 提示词段落的具名增删改；None 表示删除该段（架构方案 4.0.4）
    sections: dict[str, str | None] | None = None
    tools_added: list[ToolMeta] | None = None
    tools_removed: list[str] | None = None      # 仅工具名，对应 Pi 的 ToolReference
    timestamp: int

class UserMessage(BaseModel):
    role: Literal["user"] = "user"
    content: str | list[ContentBlock]
    timestamp: int

class AssistantMessage(BaseModel):
    role: Literal["assistant"] = "assistant"
    content: list[ContentBlock]
    api: str = ""
    provider: str = ""
    model: str = ""
    response_id: str = ""
    usage: Usage
    stop_reason: StopReason
    error_message: str = ""
    timestamp: int

class ToolResultMessage(BaseModel):
    role: Literal["tool_result"] = "tool_result"
    tool_call_id: str
    tool_name: str
    content: list[ContentBlock]
    details: dict[str, Any] = {}
    is_error: bool = False
    timestamp: int

LlmMessage = SystemMessage | UserMessage | AssistantMessage | ToolResultMessage
```

**四个内容块**：

```python
class TextBlock(BaseModel):
    type: Literal["text"] = "text"
    text: str
    text_signature: str | None = None

class ThinkingBlock(BaseModel):
    type: Literal["thinking"] = "thinking"
    thinking: str
    thinking_signature: str | None = None
    redacted: bool = False

class ImageBlock(BaseModel):
    type: Literal["image"] = "image"
    data: str
    mime_type: str

class ToolCallBlock(BaseModel):
    type: Literal["tool_call"] = "tool_call"
    id: str
    name: str
    arguments: dict[str, Any]
    thought_signature: str | None = None

ContentBlock = TextBlock | ThinkingBlock | ImageBlock | ToolCallBlock
```

### 2.2 三个 `*_signature` 字段 —— 本批次最容易被做错的地方

**它们是什么**：provider 返回的不透明串，**必须原样回传**。
典型例子是 Gemini 的 `thought_signature`、Anthropic 的 `thinking_signature`。

**漏了会怎样**：多轮对话里模型行为异常（例如工具调用后不继续、
thinking 模式报错），**但症状完全不指向根因**——你不会想到是这里丢的字段。

**本批次的硬门槛**（比"能跑通"更重要）：

> 构造一条带全部三个签名字段的 `AssistantMessage`，
> 经过 `model_dump_json()` → `model_validate_json()` 往返后，
> **三个字段逐字节不变**。

这条断言很便宜，但**它防的是一类极难排查的 bug**。

### 2.3 `base.py`

```python
class CancelToken(ABC):
    """取消信号。用 ABC 因为未来会有多种实现（signal / asyncio / 计时器）。"""

    @abstractmethod
    def is_cancelled(self) -> bool: ...
    @abstractmethod
    def raise_if_cancelled(self) -> None: ...


class BaseProvider(ABC):
    @abstractmethod
    def stream(
        self,
        messages: list[LlmMessage],
        tools: list[dict[str, Any]],
        *,
        model: str,
        signal: CancelToken,
    ) -> AsyncIterator[StreamEvent]:
        raise NotImplementedError

    @abstractmethod
    def estimate_tokens(self, messages: list[LlmMessage]) -> int:
        raise NotImplementedError
```

**硬门槛**：`BaseProvider()` 与 `CancelToken()` 直接实例化**必须抛 `TypeError`**。
这是 a2（继承制）的第一处落地验证。

### 2.4 `events.py` —— 五类流式事件

```python
class TextDelta(BaseModel):
    type: Literal["text_delta"] = "text_delta"
    text: str
    text_signature: str | None = None

class ToolCallDelta(BaseModel):
    type: Literal["tool_call_delta"] = "tool_call_delta"
    index: int
    id: str | None = None
    name: str | None = None
    arguments_delta: str = ""

class UsageEvent(BaseModel):
    type: Literal["usage"] = "usage"
    usage: Usage

class StopEvent(BaseModel):
    type: Literal["stop"] = "stop"
    stop_reason: StopReason

class ErrorEvent(BaseModel):
    type: Literal["error"] = "error"
    error: ProviderError

StreamEvent = TextDelta | ToolCallDelta | UsageEvent | StopEvent | ErrorEvent
```

**关键设计点**：工具调用是**增量**到达的（`arguments_delta` 是 JSON 片段字符串），
需要在外层累积。这个累积逻辑**放在批次 4 的 loop 里**，不在本批次。

### 2.5 `errors.py` —— 五类错误码

```python
class ErrorCode(StrEnum):
    RATE_LIMIT = "rate_limit"
    CONTEXT_OVERFLOW = "context_overflow"
    AUTH = "auth"
    TRANSIENT = "transient"
    INVALID_REQUEST = "invalid_request"

class ProviderError(Exception):
    code: ErrorCode
    message: str
    raw: dict[str, Any] = {}
    retriable: bool          # 由 code 推导，不是独立字段
```

**`context_overflow` 是重点**：它是压缩机制的两条触发路径之一
（另一条是本地 `estimate_tokens` 超阈值）。**必须在 P1 就被正确识别并向上抛**，
哪怕 P1 还没有压缩——因为识别逻辑属于 provider 层，不属于压缩层。

**`retriable` 由 `code` 推导**，不让人手填。理由：手填就会填错，
而"该重试的没重试"和"不该重试的狂重试"都是线上事故。

### 2.6 `fake.py` —— 确定性回放

```python
class FakeProvider(BaseProvider):
    """按 transcript 顺序吐事件。用于 agent loop 的离线测试。"""
```

**transcript 格式**：JSONL，每行一个"轮次"，含该轮要吐的全部事件。

**硬门槛**：**同一 transcript 回放两次，产出的事件序列逐字节一致。**
这条断言本身就是架构方案 7.2 节「确定性回放」那个设计的验证方式。

### 2.7 `openai_compat.py`

只做 OpenAI 兼容协议（a3 拍板），覆盖 DeepSeek / Kimi / GLM / 通义 / Ollama。

**必须处理的协议细节**（不做会踩）：
1. SSE 流的分块边界可能切开一个 JSON —— 需要缓冲累积
2. `[DONE]` 是结束标记，不是 JSON
3. `finish_reason` 与 `stop_reason` 要做映射
4. `usage` 默认不在流里返回，需显式请求（`stream_options.include_usage`）
5. `tool_calls[].index` 用于多工具并行时的归属

**单测策略**：用 `httpx.MockTransport`注入录制好的 SSE 字节流，
**不联网、不需要 API key**。

### 2.8 `tokens.py`

**保守估算**：`len(text) / 3` 之类，**明确标注是估算**。

**为什么不做精确 tokenizer**：不同 provider 的 tokenizer 不同，
自实现一个既不准又要维护。**真实用量以 provider 返回的 `usage` 为准。**

---

## 3. 不做什么（明确列出）

| 不做 | 为什么 | 何时做 |
| --- | --- | --- |
| 工具调用增量的累积 | 属于 loop 的职责 | 批次 4 |
| 重试逻辑 | 属于 loop 的职责 | 批次 4 |
| 上下文压缩 | P2 | P2 |
| Anthropic Provider | a3 拍板：P1 只做 1 套协议 | P4 之后 |
| 精确 tokenizer | 不准且要维护 | 不做 |
| 流式事件的 UI 渲染 | 属于产品壳 | 批次 4 |
| **`convert_to_llm()` 函数本体** | LLM 层不认识 agent 层；转换函数属 agent 层 | **批次 1.5** |
| **运行时消息类型注册表** | 同上，注册表属 agent 层 | **批次 1.5** |
| **「未知类型不静默丢弃」规则** | 它是 `convert_to_llm` 的分支行为 | **批次 1.5** |
| **压缩摘要的 `<summary>` 常量** | 压缩本身在 P2 | P2 |
| **`constrainedSampling`**（约束采样） | 属 provider 高级特性，P1 价值低 | P4（见下方说明） |

**上表后五行是 2026-09-20 补记的。** 补记原因见本节末尾「与 Pi 的对应关系」。

---

## 3.5 与 Pi 的对应关系（2026-09-20 补记）

> **补记原因**：本人问「批次 1 是否借鉴了 Pi 的设计模式」。
> 本条是回答，也是防"上下文压缩导致设计走样"的固化检查表。

**结论：批次 1 确实借鉴了 Pi，且借鉴点可逐条溯源到 `docs/pi-harness研究笔记.md` 第 9.5 节
（一手源码，Pi 0.86.0）。** 下表是完整映射。

### 3.5.1 批次 1 采用的 Pi 设计（7 项）

| # | Pi 的设计 | sigma 批次 1 的落地 | 来源 | 照抄程度 |
| --- | --- | --- | --- | --- |
| 1 | LLM 层是四个具体类型，**没有基类** | `messages.py` 四个纯 `BaseModel` | 9.5.2 | 照抄 |
| 2 | `role` 是四个值，`toolResult` 是**独立角色** | `ToolResultMessage` 独立模型（不是 assistant 的内容块） | 9.5.2 | 照抄 |
| 3 | 内容块四个 interface，靠 `type` 判别 | `TextBlock` / `ThinkingBlock` / `ImageBlock` / `ToolCallBlock` | 9.5.3 | 照抄 |
| 4 | 三个 `*Signature` 是**必须原样回传的不透明串** | 三个字段进类型 + G2 逐字节往返断言 | 9.5.3 | 照抄，**并加了断言** |
| 5 | `SystemMessage` 承担提示词演进与工具集变更 | `sections` / `tools_added` / `tools_removed` | 9.5.2 | 照抄类型，**P1 不启用** |
| 6 | `details` 不进上下文，只给程序看 | `ToolResultMessage.details` + 注释写明 | 9.5.2 | 照抄 |
| 7 | 流式事件是统一契约，UI / 落盘 / 测试共用 | `events.py` 五类判别联合 | 9.5.5 的推论 | 照抄思路 |

### 3.5.2 批次 1 **刻意不照抄**的 Pi 设计（2 项）

| Pi 的做法 | sigma 的做法 | 为什么改 | 来源 |
| --- | --- | --- | --- |
| **未知 role 静默丢弃**（`default → undefined → filter`） | **记录 warning，不静默** | Pi 的联合类型编译期已封闭，静默是安全兜底；**Python 没有编译期穷尽性检查**，静默会变成"消息莫名消失"且无从排查。与架构方案 4.4 节「不允许扩展静默覆盖内置工具」同源：**宁可崩，不要错** | 9.5.5 |
| **`IsJsonCompatible<T>` 编译期类型体操** | 运行时校验（Pydantic 自带） | 同一诉求的不同实现手段：Pi 靠编译期把不合规的类型变成 `never`，Python 只能用运行时校验。**效果相当，时机不同** | 9.5.2 |

### 3.5.3 批次 1 **暂缓**的 Pi 设计（3 项，附去向）

| Pi 的设计 | 为什么暂缓 | 去向 |
| --- | --- | --- |
| **`convertToLlm` 函数本体** | 它是 agent 层 → LLM 层的通道，**属 agent 层**，不在 provider 层 | **批次 1.5** |
| **`declare module` 式扩展机制** | TS 的编译期 declaration merging 在 Python 无对应物；改为运行时注册表 | **批次 1.5** |
| **压缩摘要 + `<summary>` 常量** | 压缩本身是 P2 的能力；但**摘要降级成 user 而非 system 这条规则现在就写进 `SystemMessage` 的 docstring** | P2（规则已前置，见 G9） |

### 3.5.4 Pi 有、sigma 明确不做的一项

| Pi 的设计 | 处置 | 理由 |
| --- | --- | --- |
| `constrainedSampling`（json_schema / grammar 约束采样） | **P1 不做**，P4 再评估 | 它让工具参数**在生成时就受约束**，而非生成后校验失败重试——"约束而非校验"确实高价值。但它依赖 provider 侧能力，P1 只有 OpenAI 兼容协议且只做最小闭环，投入产出比低 |

### 3.5.5 一条防走样的纪律

**本节存在的意义不只是回答一次提问。** 它是一张**溯源表**：
批次 1 每个设计点的来源都能查到调研笔记的具体小节。

**为什么要这样做**：本项目已经出现过一次"三版方案、读源码才定案"的情况
（见记忆里的教训）。**更危险的是另一种失真——不是推错，而是在多轮总结与压缩中
把某个规则悄悄丢掉**，且因为丢得无声无息，没人会发现。

**防范措施（本次新增）**：

1. **每个批次详规都要有本节这样的一张溯源表**，列出"采用了 Pi 的哪些设计 / 刻意不照抄哪些 / 暂缓哪些 / 明确不做哪些"。
2. **纪律**：凡是从调研笔记搬进实现规格的设计点，必须写明来源小节号。
   写不出小节号的，说明它没有来源——要么补调研，要么删掉。
3. **复查时机**：批次开工前，对照调研笔记 9.5.9 的九条结论逐条核对一遍。

---

## 4. 三处需要注意的设计取舍

### 4.1 `estimate_tokens` 是预警不是账本

**判据**：任何"我们自己算的 token 数"都只能用于**预警**（接近上限时提醒），
**不能用于报告**。评测报告里的 token 数字必须来自 provider 的 `usage`。

理由：模型换了、provider 换了，我们的估算就不准了，
而报告里的数字是给人看的结论——**估算数进报告等于造假**。

### 4.2 LLM 层不做抽象（与批次 1.5 的 agent 层形成对照）

本批次四个消息模型**都是纯 `BaseModel`，没有基类**。

理由：它们严格对应 wire protocol，字段就是协议要的样子。
加基类只会让协议转换更难写，且没有行为需要统一分派。

**对照**：批次 1.5 的 `AgentMessage` 要 `ABC`，因为它**有行为**（`to_llm()`）、
需要被统一持有、需要运行时分派。**判据是"有没有行为"，不是"重不重要"。**

### 4.3 中途 system 消息的张力（本批次要留口子）

架构方案 4.0.4 节说明：`SystemMessage` 承担提示词演进，
但**中途插入 system 消息会破坏 prompt cache**（D4 节要求常驻区逐字节稳定）。

**本批次的处置**：类型上**保留** `sections` / `tools_added` / `tools_removed` 三个字段，
但 `OpenAICompatProvider` **P1 只在会话开始处发一条 system 消息**。
中途变更的能力**保留类型但不启用**，等 P2 做压缩时一并设计缓存策略。

**要写的转换逻辑**：OpenAI 兼容协议接受中途 system 消息则原位发；
不接受的 provider（未来加 Anthropic 时）需按重放状态重建首条 system 消息。
**这条逻辑 P1 就要写**，不能推——它是类型的一部分。

---

## 5. 硬门槛汇总（本批次验收）

| # | 门槛 | 为什么这条 |
| --- | --- | --- |
| G1 | `BaseProvider()` / `CancelToken()` 直接实例化抛 `TypeError` | a2 继承制的第一处落地验证 |
| G2 | 带三个 `*_signature` 的消息 JSONL 往返后逐字节不变 | 防一类极难排查的 bug |
| G3 | 五类 `StreamEvent` 逐类 round-trip 序列化 | 事件是 UI / 落盘 / 测试的公共契约 |
| G4 | `FakeProvider` 同 transcript 两次回放逐字节一致 | 确定性回放的前提 |
| G5 | 全部单测在无 API key 环境下通过 | 与 P0 门槛一致 |
| G6 | `context_overflow` 能被识别并抛出 | 压缩的两条触发路径之一，P2 要用 |
| G7 | 三道门禁（lint-imports / mypy / pytest）保持全绿 | 每批次固定门槛 |
| **G8** | **`sigma_ai` 的 import-linter 契约不破**：`sigma_ai` 不得 import `sigma_agent` | 消息层不混用；`sigma_ai` 是依赖图最底层，不允许它认识 `AgentMessage` |
| **G9** | **`SystemMessage` 的定义里没有任何"压缩摘要"语义的字段** | 摘要必须降级成 `user`（架构方案 4.0.6 / 第 8 节门禁「摘要降级位置」） |

### G8 / G9 是 2026-09-20 补记的

**G8**：批次 1 会新增 `messages.py`，它有很强的动机去 import `sigma_agent`
（比如想写 `def to_llm(self)` 就把 `AgentMessage` 引进来）。**必须从第一天就钉死。**
P0 已有这条契约（分层契约覆盖），但批次 1 详规原先没把它列为本批次门槛。

**G9 的来由 —— 一个真实的踩坑风险**：

批次 1 定义 `SystemMessage` 时有三个字段（`sections` / `tools_added` / `tools_removed`），
它们让 `SystemMessage` 看起来"可以承载任何想注入上下文的东西"。
实施时把**压缩摘要拼进 `SystemMessage.content`** 是非常自然的选择
——因为**没有任何东西阻止这么做**。

但架构方案 4.0.6 节明确规定：**摘要降级成 `user`，不是 `system`。**
理由是 system 消息占常驻区，而常驻区在会话内**必须逐字节稳定**（D4）。
把摘要写进 system 消息 = 常驻区中途变化 = **prompt cache 从变动点起全部失效**。

**症状不会在本批次暴露**，要等到 P2 做压缩时才会以"成本异常上升"的形式出现，
而那时已经很难定位到 `SystemMessage` 的定义上。

**所以 G9 的落地形式**：在 `messages.py` 的 `SystemMessage` 类 docstring 里
写一条显式警告（见 2.1 节补充），并加一个单测断言
`SystemMessage` 的字段集合**恰好**是约定的那些，新增字段时测试失败。

这条门禁**成本极低**（一个字段集合断言），防的是一个跨阶段的隐蔽错误。

---

## 6. 预估工作量与批次划分

| 子步骤 | 内容 | 依赖 |
| --- | --- | --- |
| 1a | `messages.py` + 单测（G2） | 无 |
| 1b | `base.py` / `errors.py` + 单测（G1 / G6） | 无 |
| 1c | `events.py` + 单测（G3） | 1a |
| 1d | `fake.py` + 单测（G4） | 1b / 1c |
| 1e | `openai_compat.py` + 单测（mock transport） | 1a / 1b / 1c |
| 1f | `tokens.py` + 门禁复查 | 1a |

**建议执行顺序：1a → 1b → 1c → 1d → 1e → 1f。**
先 1a/1b（无依赖），再 1c/1d（事件与回放），1e 放最后（要联网协议细节但用 mock 测）。

**本批次不写 `docs/plans/P1-最小闭环.md` 的验收记录**——
那是批次 5 的事。本批次完成后只在本文件末尾追加"批次 1 验收证据"小节。

---

## 7. 需要你确认的两点

| 编号 | 问题 | 我的建议 |
| --- | --- | --- |
| B1.1 | `tokens.py` 的估算策略 | `len(str) / 3` + 明确标注"估算"；**不做 tokenizer** |
| B1.2 | `openai_compat.py` 的单测方式 | 用 `httpx.MockTransport` 注入录制 SSE；**不引入 respx 等额外依赖** |

**另有一项提醒**：本批次会**第一次真正用到 `httpx`**（已在依赖里）。
如果你希望 P1 完全不出现网络代码，可以改成只写 `FakeProvider`，
`OpenAICompatProvider` 推到批次 4 接线时再写——**但这会让 `FakeProvider`
成为唯一验证过的 provider，是有风险的**（回放测试通过不代表真实协议能跑）。
我的建议是按原计划做，用 mock transport 保证离线可测。
