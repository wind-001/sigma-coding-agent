# 技术债登记（sigma）

> 建立：2026-09-25（配合 `docs/plans/Review-2026-09-24-缺陷修复批次-详规.md` 收口后的清理）
> 维护规则
>   1. **只登记有凭据的债**：路径（+行号）+ 现象 + 判据/复现方式。不登记"想重构"这类无判据的愿望。
>   2. **清掉一条就删掉该条**，不写"已完成"历史——历史属于 commit message 与各详规的实施记录。
>   3. 每条必须能回答一个问题：**留着它会出什么事？** 答不上来的不进本文件。

---

## 1. 已清理（2026-09-25，全部零行为变更）

| # | 内容 | 凭据 / 判据 |
| --- | --- | --- |
| 1 | `tests/test_session_tree.py` 里 `test_invalid_utf8_line_is_skipped_not_fatal` **逐字节重复定义两次**（原 502 / 529 行） | 后定义静默覆盖前定义，pytest 只收集一条 → 重复代码零收益，且让"本批新增 31 条回归测试"的计数虚高一条。已删第二份（−27 行），全量仍 638 passed（该函数本就只跑一次） |
| 2 | 11 处**未使用的 import** | 判据：把 import 语句所在行剔除后，该名字在文件其他地方**一次都没出现**（含注释与字符串注解；只有漏报、不会误报）。清单：`core/sigma_tools/grep.py`(`Path`)、`scripts/gate_injection_p2_compaction.py`(`Path`)、`tests/test_session_compact.py`(`estimate_messages`)、`tests/test_session_tree.py`(`warnings`)、`tests/test_sigma_repl.py`(`SESSION_DIR_HINT`)、`tests/test_sub_agent.py`(`BaseModel`/`Field`/`BaseTool`/`ToolResult`/`TextBlock`)、`tests/test_todo_steering.py`(`pytest`，该文件靠 `asyncio_mode=auto` 跑，无任何 `pytest.` 调用) |
| 3 | `.import_linter_cache.bak/`（import-linter 缓存的一次性备份目录，8 个文件） | import-linter 会在自己的缓存目录里写 `.gitignore`(`*`)，所以它既不入库也不出现在 `git status`——纯垃圾。`lint-imports` 只认 `.import_linter_cache/`，删除无影响（清理后 3 kept 复验） |
| 4 | 本地测试残渣：`.pytest_cache/`（6.1 MB，含 `garbage-*` 与历史 `pytest-N/` 目录）、`.pytest-tmp/`（`codex-run`、`pytest-of-*`）、`.ptb/`、根 `__pycache__/` | 全是可再生的构建/运行产物（已在 `.gitignore` 内），只影响本机噪音与磁盘 |

复验（清理后）：`pytest tests -q` = 638 passed；`mypy core` = Success（56 files）；`lint-imports` = 3 kept, 0 broken。

**这套静态检查怎么复现**（当前是一次性脚本，未入库；是否固化成门槛见 D4）：
对每个 `.py` 用 `ast` 取顶层 `FunctionDef/AsyncFunctionDef/ClassDef` 名字分组，`len>1` 即重复定义；
取 `Import/ImportFrom`（跳过 `__future__`）的名字，剔除 import 行后在正文里做词边界搜索，搜不到即未使用。

---

## 2. 已决定（2026-09-25，备忘；不留债条目）

- **D1 `scripts/quick_sort.py`**：已删除——上一轮实验草稿，全库无任何引用（删前已确认）。
- **D2 未跟踪的项目产物**：已 `git add` 四个文件——`docs/plans/Review-2026-09-24-缺陷修复批次-详规.md`、
  `tests/test_sigma_ai_tool_calls.py`（H 组 3 条）、`tests/test_tools_write_read.py`（G 组 5 条）、本文件。
  （提交时机由本人决定；`git add` 只是让它们不再"只存在于本机工作区"。）
- **D3 "低危 9 条清单"的悬空引用**：**已按"如实标注遗失"处置**（本人选方案 2）。
  详规第 6 行改为"该清单不在仓库里、已不可恢复"，并在该文件 §0.1 实施记录第 5 条写明核实范围；
  残留影响（下一轮 review 会重报这 9 条）登记在下面 §3。
- **D4 静态卫生门槛测试**：**暂不采纳**（本人决定，2026-09-25）。理由：本轮两类问题已一次性清零，
  且仓库已有 `mypy strict` 与详规纪律；将来若再出现同类重复定义 / 僵尸 import，
  按本文件第 1 节末尾那段方法临时重跑即可，不必常驻一条门禁。

---

## 3. 已知、但暂不处理（有凭据，低优先）

| 项 | 凭据 | 为什么先不动 |
| --- | --- | --- |
| `core/sigma_tools/_credit_ledger.py` 无**直接**测试 | 全库 grep 无 `_credit_ledger`/`CreditLedger` 出现在 `tests/`；它经 `FirecrawlQuota`(18 处)/`TavilyQuota`(24 处) 间接覆盖 | 抽象基类的公共逻辑已被两个子类的测试走通；单独补测收益低于成本 |
| `core/sigma/__main__.py` 无测试 | `tests/test_sigma_cli.py` 只调 `cli.main()`，没有 `python -m sigma` 的进程级用例 | 该文件是入口 shim；进程级用例需要子进程 + 环境变量，成本高。真要补，应在 CI 里加一条 smoke |
| grep 超时后的**孤儿线程**会继续占 CPU | `core/sigma_tools/grep.py` 的 `asyncio.to_thread` + `wait_for`：超时只解除等待，线程里的回溯照跑（`(a+)+$` 对 24 个 'a' 实测 ~1.8 s；更大输入更久），进程退出时 join 会等它 | 详规 Q4 已明确不引 `regex` 依赖；这是被接受的取舍（上限由"单文件 10 s + 文件数"约束） |
| `read` 在**空文件**上 `start_line=1` 现在报错（"一共只有 0 行"） | `core/sigma_tools/read.py` 越界校验：`start_line > total_lines` → `is_error` | 新增错误路径是 G3 修复的必然结果；文案已给总行数，信息量足够 |
| `truncate_output` 的 marker 不再报"头 N 行 / 尾 M 行" | `core/sigma_tools/truncate.py`：文案改为"头尾各若干行"，实际条目数在 `details.kept_lines/total_lines` | 预算硬上限（G2）要求 marker 长度可预测；信息未丢，只是从 content 挪到 details |
| 6 处 `# type: ignore` | `core/` 下共 6 处 | `mypy strict` 的 `warn_unused_ignores=True` 保证它们都还必要，不是僵尸注释 |
| `scripts/desktop/kill-my-apps.bat` 对 git **不可见** | `.gitignore` 的 `desktop/` 规则命中它（`git ls-files scripts/desktop` 为空），但它躺在受版本控制的 `scripts/` 里 | 那个 `.gitignore` 条目写明"本地桌面工具脚本，不属于项目"，所以这是**有意**的；只提醒一句：想共享就得挪出 `desktop/`，否则别人 clone 不到它 |
| 原 review 的**低危 9 条**清单已遗失（不可恢复） | 详规第 6 行现如实标注；核实范围见该文件 §0.1 第 5 条（全库 270+ 文本文件 / `git log --all -S低危` / 被 ignore 的 `.workbuddy/memory/`，三路皆无） | 不可行动项：只能靠下一轮全库 review 重新发现，届时与"已修 18 条"的去重只能靠人脑。**这项信息损失已被接受**——详规从未宣称修过这 9 条，第 6 行的"不在本批"是范围边界 |