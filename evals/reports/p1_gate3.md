# P1 验收门第 3 条：10 条评测任务 + B1 vs B2 对照（试水报告）

> 生成时间 2026-09-23T12:46:27 ｜ 判定器 `PytestJudge`（判定前用原始测试覆盖回 `tests/`）

## 档位

| 档 | 含义 |
| --- | --- |
| B0 | 无工具、单轮——健全性检查：它若能过，说明任务不需要 agent |
| B1 | 有工具、满上下文、**无**压缩、无 checkpoint（对照组） |
| B2 | 完整 harness（主结果档） |

## 逐任务对照

| 任务 | 内容 | B0 | B1 | B2 | B1→B2 |
| --- | --- | --- | --- | --- | --- |
| syn-001 | 按显示宽度折行（东亚宽度） | ✓ | ✓ | ✓ | 同过 |
| syn-002 | 金额分摊不丢账（最大余数法） | ✓ | ✓ | ✓ | 同过 |
| syn-003 | 单行 CSV 解析（RFC 4180 引号规则） | ✓ | ✓ | ✓ | 同过 |
| syn-004 | 二分查找的两处边界（index_of / insertion_point） | ✓ | ✓ | ✓ | 同过 |
| syn-005 | LRU 缓存：淘汰的是「最久未使用」而不是「最先写入」 | ✓ | ✓ | ✓ | 同过 |
| syn-006 | 闰年漏了整百年规则（1900 不是闰年） | ✓ | ✓ | ✓ | 同过 |
| syn-007 | 模板渲染：占位符互相独立 + 缺失 key 要炸出来 | ✓ | ✓ | ✓ | 同过 |
| syn-008 | 通配符匹配：* 是「零个或多个」不是「一个或多个」 | ✓ | ✓ | ✓ | 同过 |
| syn-009 | 保序去重：casefold 合并重复但要保留首现原样 | ✓ | ✓ | ✓ | 同过 |
| syn-010 | 换行归一化：CRLF 不能变成两个 LF | ✓ | ✓ | ✓ | 同过 |

## 成本（口径与 runner.py 对齐）

| 档 | 通过 | 调用轮数合计 | prompt tok 合计 | completion tok 合计 | wall-clock 合计 |
| --- | --- | --- | --- | --- | --- |
| B0 | 10/10 | 10 | 5,273 | 2,851 | 14.3s |
| B1 | 10/10 | 78 | 252,187 | 11,721 | 159.7s |
| B2 | 10/10 | 83 | 278,518 | 11,844 | 171.1s |

## 配对结果（B1 vs B2）

- 配对数 n = **10**
- **回退**（B1 过、B2 不过）：**0** 条
- 增益（B1 挂、B2 过）：0 条

**⚠️ 这份报告不能下结论。** 按 P2-6 详规 §11.2 的 Wilson 单侧 95% 上界：
70 条任务只容 **3 条**回退；**10 条试水容差为 0**。
试水的用途只有三个：链路通了、档位开关真的在改变行为、方向可看。

## 试水发现（算出来的，不是态度）

1. **B0 通过 10/10。**B0 = 无工具、单轮、只看任务描述和源码。它全过说明：**这批任务「读代码+一次写对」不需要任何 agent**——描述把症状说得太准，单文件又足够小。任务集对 B0 **没有区分度**（详规 R3/R6 预警的风险，实测坐实）。70 条正式集必须改：描述降噪（不给症状细节，让 agent 自己读测试定位）+ 提高隐蔽性，否则整套评测量不出 harness 的价值。
2. **B1 与 B2 在本批任务上无差异**（全部同过，n=10）。这不是开关失效：单条任务的历史峰值远低于压缩触发线（动态区 ≈ 25.6k token），**压缩全程未触发，B1 与 B2 本来就该一样**。开关自身的'改变行为'由注入实验G80（删掉显式关断分支必须红）钉住；'B1 vs B2 的成功率差异'要等**任务长到能触发压缩**的正式集（或恢复 F 类长上下文任务）才测得到。
3. **三档消耗拉开了量级差**：B0 单轮（rounds=1），agent 档最少 7 轮——工具循环确实在做事，不是模型一答了之。

## 结论位（P1 门第 3 条）

- [x] 链路：10 条任务 × 3 档全部跑完、判定有证据 → 见逐任务对照
- [x] 档位可消融：B0 与 agent 档的行为差被观察到（单轮 vs 多轮）；B1/B2 无差异属预期（压缩未触发），开关行为由 G80/G81 注入钉住
- [ ] 正式结论：**待 70 条任务集**（P2 门第 3 条），且任务难度要先改（见发现 1）
